// this is a constant for all the colors used int applicationconst white = Colors.white;
import 'package:flutter/material.dart';

const white = Colors.white;
const normalBlack = Colors.black;
final grey = Colors.grey[900];
const transparent = Colors.transparent;
const Color green = Color.fromRGBO(146, 202, 52, 1);
const error = Colors.pink;
final red = Colors.red[900];
const Color pink = Color.fromRGBO(231, 30, 103, 1);
const Color button = Color.fromRGBO(51, 51, 57, 1);
const Color black = Color.fromRGBO(28, 28, 35, 1);
Color menuIcon = Color(int.parse("0xffB2D0CE"));
const textFieldText = Color.fromRGBO(196, 196, 196, 1);
const black51 = Color.fromRGBO(51, 51, 57, 1);
const brown = Colors.brown;
